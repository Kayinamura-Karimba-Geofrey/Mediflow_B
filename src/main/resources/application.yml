spring:
  application:
    name: Mediflow_B
  datasource:
    url: jdbc:postgresql://localhost:5432/mediflow
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: geofreykayin@gmail.com
    password: fnkryjtznqzcowhk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

mediflow:
  app:
    jwtSecret: "======================MediflowSecretKeyForJWTAuthentication======================"
    jwtExpirationMs: 86400000

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha
    tagsSorter: alpha
